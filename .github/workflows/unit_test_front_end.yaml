name: Jest Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14.x'
      - name: Install Jest
        run: npm install --save-dev jest
      - name: Run Jest tests
        working-directory: ${{ github.workspace }}/Application_SE02/front-end/src/__test__
        run: npm test -- --testPathPattern=".*" && exit 0
